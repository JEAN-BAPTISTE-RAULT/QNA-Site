{%
  set classes = [
    'node',
    'node--type-' ~ node.bundle|clean_class,
    node.isPromoted() ? 'node--promoted',
    node.isSticky() ? 'node--sticky',
    not node.isPublished() ? 'node--unpublished',
    view_mode ? 'node--view-mode-' ~ view_mode|clean_class,
  ]
%}
{{ attach_library('qna_theme/node') }}
<article{{attributes.addClass(classes)}}>
	{{ title_prefix }}
	{% if label and not page %}
		<h2{{title_attributes}}>
			<a href="{{ url }}" rel="bookmark">{{ label }}</a>
		</h2>
	{% endif %}
	{{ title_suffix }}

	{% if display_submitted %}
		<footer class="node__meta">
			{{ author_picture }}
			<div{{author_attributes.addClass('node__submitted')}}>
				{% trans %}Submitted by
				{{ author_name }}
				on
				{{ date }}{% endtrans %}
				{{ metadata }}
			</div>
		</footer>
	{% endif %}

	<div class="block-about__container" {{ content_attributes.addClass('node__content') }}>
			{% include 'themes/qna_theme/templates/custom/common/page-header.html.twig' with {
      title: node.field_page_title.value,
      description: node.body.value,
      share: node.field_share_on_social_medias.value,
      url: url('entity.node.canonical', {'node': node.id() }, {'absolute': true})
    } %}

		<div>
			{% for item in node.field_paragraphs %}
				<div {% if not loop.first %} {% endif %}>
					{{ item | view }}
				</div>
			{% endfor %}
		</div>
	</div>
</article>
